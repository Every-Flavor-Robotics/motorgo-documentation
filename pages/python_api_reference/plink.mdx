import PythonFunctionDoc from "/components/PythonFunctionDoc.tsx";
import PythonClassDoc from "/components/PythonClassDoc.tsx";



<PythonClassDoc
    title="Plink"
    description="The Plink class handles communication with the Plink when connected as
    a PiHat. The class provides interfaces for controlling the 4 motor channels,
    ready the encoder data, and reading the IMU sensor data."
    attributes={[
        { name: 'channel1', type: 'MotorChannel', description: 'Interface for controlling motor channel 1.' },
        { name: 'channel2', type: 'MotorChannel', description: 'Interface for controlling motor channel 2.' },
        { name: 'channel3', type: 'MotorChannel', description: 'Interface for controlling motor channel 3.' },
        { name: 'channel4', type: 'MotorChannel', description: 'Interface for controlling motor channel 4.' },
        { name: 'imu', type: 'IMU', description: 'Interface for reading IMU sensor data.' },
    ]}
    methods={[
        {
            name: 'my_method',
            signature: 'def my_method(self, param1: int, param2: str) -> bool:',
            description: 'Performs an example operation.',
            parameters: [
                { name: 'param1', type: 'int', description: 'The first parameter.' },
                { name: 'param2', type: 'str', description: 'The second parameter.' },
            ],
            returns: {
                type: 'bool',
                description: 'True if successful, False otherwise.',
            },
            raises: [
                { type: 'ValueError', description: 'If param1 is negative.' },
            ],
        },
    ]}
    examples={[
        {
            code: `obj = MyClass()\nresult = obj.my_method(42, "hello")\nprint(result)`,
            language: 'python',
        },
    ]}
/>

<PythonFunctionDoc
    title="plink.__init__"
    signature="__init__(self, frequency: int = 200, timeout: float = 1.0)"
    description="Initializes the Plink object."
    parameters={[
    ]}
    keywordArguments={[
        { name: 'frequency', type: '(int, Optional)', description: 'Communication frequency with the Plink. The update frequency tends to become unstable past 200 Hz.', default: '200' },
        { name: 'timeout', type: '(float, Optional)', description: 'Communication timeout (seconds). If no data is received beyond this time, the Plink is reset.', default: '1.0' }
    ]}

/>

<PythonFunctionDoc
    title="plink.reset"
    signature="reset(self)"
    description="Resets the Plink device. This toggles the reset pin on the ESP32, causing its program to restart. Useful if the Plink is unresponsive or misbehaving."
    parameters={[
    ]}
    keywordArguments={[
    ]}

/>

<PythonFunctionDoc
    title="plink.connect"
    signature="connect(self)"
    description="Begins communication with the Plink. Initializes the communication thread, confirms the connected device is a MotorGo Plink, and starts periodic data transfer. The power supply voltage limit must be set prior to calling this."
    parameters={[
    ]}
    keywordArguments={[
    ]}
    raises={[
        { type: 'ValueError', description: 'If the voltage limit is not set before connecting.' }
    ]}

/>