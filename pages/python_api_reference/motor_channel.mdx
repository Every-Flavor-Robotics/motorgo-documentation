import PythonFunctionDoc from "/components/PythonFunctionDoc.tsx";
import PythonClassDoc from "/components/PythonClassDoc.tsx";



<PythonClassDoc
    title="MotorChannel"
    description="Interface for a single motor channel. The MotorChannel class provides an interface for controlling a single motor channel. It supports controlling the motor in power mode or velocity mode. For velocity control, the class exposes the PID control parameters for tuning the controller. The MotorChannel class also provides access to read the current position and velocity of the motor. All of the commands and encoder data are set and accessed as properties of the class, rather than through function calls."
    attributes={[
        { name: 'control_mode', type: 'ControlMode', description: 'The control mode for the motor channel. The control mode can be set to POWER, VELOCITY, or NONE.' },
        { name: 'velocity_command', type: 'float', description: 'The velocity command for the motor channel. Only used when control_mode is set to VELOCITY.' },
        { name: 'power_command', type: 'float', description: 'The power command for the motor channel. Only used when control_mode is set to POWER.' },
        { name: 'position', type: 'float', description: 'The current position of the motor.' },
        { name: 'velocity', type: 'float', description: 'The current velocity of the motor.' },
        { name: 'velocity_p', type: 'float', description: 'The proportional gain for the velocity PID controller.' },
        { name: 'velocity_i', type: 'float', description: 'The integral gain for the velocity PID controller.' },
        { name: 'velocity_d', type: 'float', description: 'The derivative gain for the velocity PID controller.' },
        { name: 'velocity_output_ramp', type: 'float', description: 'The output ramp rate for the velocity PID controller.' },
        { name: 'velocity_lpf', type: 'float', description: 'The low-pass filter constant for the velocity PID controller.' },
    ]}
    methods={[
    ]}
    examples={[
        {
            code: `obj = MyClass()\nresult = obj.my_method(42, "hello")\nprint(result)`,
            language: 'python',
        },
    ]}
/>

<PythonClassDoc
    title="ControlMode"
    description="Enum for setting the control mode of the motor channel.
    The ControlMode enum defines the possible control modes for the motor channel. The control mode can be set to POWER for controlling the motor
    with a power command, VELOCITY for controlling the motor with a velocity command, or NONE for no control. When the control mode is set to VELOCITY,
    you must also set the PID gains for the velocity controller for the motors to operate correctly. When the control mode is set to POWER, the motor
    will be controlled with a power command between -1.0 and 1.0. When the control mode is set to NONE, the motor channel will be disabled.

    example:
        from motorgo import ControlMode
        motor_channel.control_mode = ControlMode.POWER
        motor_channel.control_mode = ControlMode.VELOCITY"
    attributes={[
        { name: 'VELOCITY', type: 'int', description: 'Used to set the control mode for velocity control.' },
        { name: 'POWER', type: 'int', description: 'Used to set the control mode for power control.' },
        { name: 'NONE', type: 'int', description: 'Used to set the control mode to none.' },
    ]}
    methods={[
    ]}
    examples={[
        {
            code: `from motorgo import ControlMode
motor_channel.control_mode = ControlMode.POWER
motor_channel.control_mode = ControlMode.VELOCITY`,

            language: 'python',
        },
    ]}
/>